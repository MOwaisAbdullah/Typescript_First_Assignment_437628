let transport: string[]=['Honda Motorcycle','Honda City Car','Honda Civic Car','Greenline Bus','Toyota Corolla'];
let message: string='“I would like to own a `   ';
for (let index in transport){
    console.log(message+transport[index]+'."');

}