var transport = ['Honda Motorcycle', 'Honda City Car', 'Honda Civic Car', 'Greenline Bus', 'Toyota Corolla'];
var message = '“I would like to own a ';
for (var index in transport) {
    console.log(message + transport[index] + '."');
}
